class VirtualButtonMatrix < NSMatrix
  # 
  # def mouseDown(event)
  #   row = Pointer.new_with_type('q')
  #   column = Pointer.new_with_type('q')
  #   if getRow(row, column:column, forPoint:convertPoint(event.locationInWindow, fromView:nil))
  #     row = row[0]
  #     column = column[0]
  #     cell = cellAtRow(row, column:column)
  #     cell.trackMouse(event, inRect:cellFrameAtRow(row, column:column), ofView:self, untilMouseUp:true)
  #   end
  # end
  # 
end